import Image from 'next/image'; import ProductCard from '@/components/ProductCard'; import USP from '@/components/USP'; import { products } from '@/lib/products';
export const dynamic = 'force-dynamic';
export default function Category({ params }: { params: { category: string } }) { const cat = params.category.toUpperCase(); const list = products.filter(p => cat === 'SALE' || p.category === cat || cat === 'ACCESSORIES' || cat === 'PRE-ORDER'); return <main><section className="container mt-5"><div className="card overflow-hidden"><div className="relative h-56"><Image src="/banners/category.svg" alt="category" fill className="object-cover" /><div className="absolute p-8"><p>Trang chủ › {cat}</p><h1 className="text-4xl font-black">{cat} COLLECTION</h1><p>Dòng sản phẩm chính hãng, chi tiết sắc nét, nhiều ưu đãi.</p></div></div></div><div className="grid md:grid-cols-[260px_1fr] gap-5 mt-5"><aside className="card p-5 h-fit"><b>BỘ LỌC SẢN PHẨM</b>{['Danh mục', 'Giá', 'Tình trạng', 'Thương hiệu', 'Sắp xếp'].map(h => <div className="border-t mt-4 pt-4"><b className="text-sm">{h}</b><label className="block mt-2 text-sm"><input type="checkbox" defaultChecked /> Còn hàng</label><label className="block mt-2 text-sm"><input type="checkbox" /> Pre-order</label></div>)}<button className="btn btn-dark w-full mt-5">ÁP DỤNG BỘ LỌC</button></aside><section><div className="card p-4 flex justify-between mb-5"><span>Hiển thị 1-{list.length} của {products.length} sản phẩm</span><select className="border rounded-lg px-3"><option>Mới nhất</option><option>Bán chạy</option><option>Giá thấp - cao</option></select></div><div className="grid-products">{(list.length ? list : products).map(p => <ProductCard key={p.id} p={p} />)}</div><div className="flex justify-center gap-2 mt-6"><button className="btn btn-red">1</button><button className="btn bg-white">2</button><button className="btn bg-white">3</button></div></section></div></section><USP /></main> }
