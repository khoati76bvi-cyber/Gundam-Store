import Image from 'next/image'; import { products, formatPrice } from '@/lib/products';
export const dynamic = 'force-dynamic';
export default function Compare() { const list = products.slice(0, 4); return <main className="container mt-8"><h1 className="text-4xl font-black">So sánh sản phẩm</h1><div className="card overflow-auto mt-5"><table className="w-full text-sm"><thead><tr>{list.map(p => <th className="p-4" key={p.id}><Image src={p.image} alt={p.name} width={150} height={120} className="bg-black rounded-xl mx-auto" /><b>{p.name}</b></th>)}</tr></thead><tbody>{['category', 'price', 'rating', 'stock', 'brand'].map(k => <tr className="border-t" key={k}>{list.map(p => <td className="p-4 text-center" key={p.id + k}>{k === 'price' ? formatPrice(p.price) : (p as any)[k]}</td>)}</tr>)}</tbody></table></div></main> }
